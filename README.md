# Team Blue Management App
## Installation
1. Run `npm install` in /backend and /frontend
2. Start MongoDB
3. Run `node index.js` in /backend
4. Run `npm start` in /frontend
## API Endpoints
- GET /api/members (View all)
- POST /api/members (Add new)